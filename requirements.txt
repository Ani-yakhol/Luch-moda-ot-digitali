# לוח מודעות דיגיטלי — Digital Bulletin Board v4.5
# התקנה: pip install -r requirements.txt

# ── חובה ──────────────────────────────────────────────────────────────────────
Pillow>=9.0.0          # עיבוד תמונות (PIL)
pyluach>=1.4.0         # לוח עברי — חישוב תאריכים עבריים, פרשיות, מועדים
astral>=3.2            # חישוב שעות זריחה ושקיעה (גיאוגרפיה)
pytz>=2022.1           # אזורי זמן
PyQt6>=6.4.0           # ממשק ניהול (GUI)
zmanim>=0.2.0          # זמני הלכה — KosherJava Python port (מדויק יותר)

# ── אופציונלי — תמיכה ב-PDF ──────────────────────────────────────────────────
# pdf2image>=1.16.0    # המרת PDF לתמונות (דורש גם התקנת poppler)

# ── אופציונלי — תמיכה ב-MP4/וידאו ───────────────────────────────────────────
# opencv-python>=4.5.0  # הפעלת קבצי וידאו MP4
